name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Deploy (placeholder)
        run: |
          echo "Deploying the application..."
          # Add your deploy commands here, e.g.:
          # scp, rsync, docker build/push, kubectl apply, etc.
