name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - ".github/workflows/ci.yml"
      - ".github/workflows/cd.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run tests or checks
        run: echo "✅ Running CI for non-workflow changes"
